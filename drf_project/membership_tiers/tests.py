from django.test import TestCase
from drf_project.membership_tiers.models import Membership, UserMembership

# Create your tests here.


# class MemberTestCase(TestCase):
# def test_animals_can_speak(self):
#"""Animals that can speak are correctly identified"""
#check_membership = Membership.objects.get(choices="Premium")
#cat = Membership.objects.get(name="cat")
# self.assertEqual(Membership.membership_type.choices(),
# 'User has Premium membership')
